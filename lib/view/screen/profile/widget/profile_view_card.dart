import 'package:flutter/material.dart';

class ProfileViewCard extends StatelessWidget {
  const ProfileViewCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Row();
  }
}
